{
  "name": "arman-puzzle-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker + Durable Object backing the cloud-placement puzzle",
  "scripts": {
    "dev": "wrangler dev --port 8799",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.86.0"
  }
}
