{
  "note": "EXAMPLE placeholder so `tsc` can resolve the import in CI / fresh checkouts. The real worker/src/targets.json holds the secret answer coordinates, is generated by scripts/generate-pieces.mjs, and is gitignored. Copy this to targets.json (or run the generator) to typecheck locally.",
  "radius": 0.08,
  "tolerance": 0.06,
  "targets": [{ "x": 0.5, "y": 0.35 }]
}
